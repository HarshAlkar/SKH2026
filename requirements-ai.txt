# Training extras for VitalReach AI (symptoms / skin / livestock).
# Install in a venv used for training; Django runtime can use sklearn + tflite-runtime only.
tensorflow>=2.16.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
Pillow>=10.0.0
xgboost>=2.0.0
lightgbm>=4.0.0
kagglehub>=0.3.0
